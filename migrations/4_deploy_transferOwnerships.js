const LlamaToken = artifacts.require("LlamaToken");
const MasterShepherd = artifacts.require("MasterShepherd");
const Timelock = artifacts.require("Timelock");

module.exports = async function (deployer) {   
    // const Lama = await LlamaToken.deployed();
    // const Shepherd = await MasterShepherd.deployed();
    // const TimelockInstance = await Timelock.deployed();

    //await Lama.transferOwnership(Shepherd.address)
    //await Shepherd.transferOwnership(TimelockInstance.address)
    
  }